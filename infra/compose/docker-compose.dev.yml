name: kuuna-dev

services:
  dashboard:
    build:
      context: ../../apps/dashboard
      dockerfile: Dockerfile.dev
    working_dir: /app
    command: sh -c "npm install && rm -rf .next && npm run dev -- --hostname 0.0.0.0 --port 3000"
    env_file:
      - ../env/dashboard.env.example
    ports:
      - "3000:3000"
    volumes:
      - ../../apps/dashboard:/app
      - ../../data/dashboard/node_modules:/app/node_modules
    depends_on:
      backend:
        condition: service_started

  backend:
    build:
      context: ../../backend
      dockerfile: Dockerfile.dev
    working_dir: /app
    command: sh -c "uv sync && uv run alembic upgrade head && uv run uvicorn kuuna_backend.main:app --host 0.0.0.0 --port 8000 --reload"
    env_file:
      - ../env/backend.env.example
      - ../env/backend.env.local
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    ports:
      - "8000:8000"
    volumes:
      - ../../backend:/app
      - ../../data/backend/.venv:/app/.venv
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
      runtime-agent:
        condition: service_started

  worker:
    build:
      context: ../../backend
      dockerfile: Dockerfile.dev
    working_dir: /app
    command: sh -c 'uv sync && uv run watchfiles --filter python "uv run rq worker default" src'
    env_file:
      - ../env/backend.env.example
      - ../env/backend.env.local
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    volumes:
      - ../../backend:/app
      - ../../data/worker/.venv:/app/.venv
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      runtime-agent:
        condition: service_started

  runtime-agent:
    build:
      context: ../../services/runtime-agent
      dockerfile: Dockerfile
    working_dir: /app
    command: sh -c "uv sync && uv run uvicorn app:app --app-dir src --host 0.0.0.0 --port 8100 --reload"
    env_file:
      - ../env/backend.env.example
      - ../env/backend.env.local
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    ports:
      - "8100:8100"
    volumes:
      - ../../services/runtime-agent:/app

  gateway:
    build:
      context: ../../services/gateway
      dockerfile: Dockerfile.dev
    working_dir: /app
    command: sh -c 'uv sync && uv run watchfiles --filter python "uv run python src/app.py" src'
    env_file:
      - ../env/gateway.env.example
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    volumes:
      - ../../services/gateway:/app
      - ../../data/gateway/.venv:/app/.venv
      - ../../data/gateway/session:/data
    depends_on:
      backend:
        condition: service_started

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: kuuna
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ../../data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kuuna"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: miniopassword
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ../../data/minio:/data
